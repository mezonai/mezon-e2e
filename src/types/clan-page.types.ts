export enum ChannelType {
  TEXT = 'TEXT',
  VOICE = 'VOICE',
  STREAM = 'STREAM',
}

export enum ChannelStatus {
  PUBLIC = 'PUBLIC',
  PRIVATE = 'PRIVATE',
}

export enum ThreadStatus {
  PUBLIC = 'PUBLIC',
  PRIVATE = 'PRIVATE',
}