export enum OnboardingTask {
  SEND_FIRST_MESSAGE = 'sendFirstMessage',
  INVITE_PEOPLE = 'invitePeople',
  CREATE_CHANNEL = 'createChannel',
  TICK_GREEN = 'tickGreen',
}

export type OnboardingTaskType = OnboardingTask;
